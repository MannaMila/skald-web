# Native 0.7.0 screenshots on the Skald landing page (2026-09-15)

This is a scoped copy of exactly 7 files from `MannaMila/mannamila-web` `skald/`: `index.html`, `styles.css`, `assets/reader-art.webp`, `assets/greek-split.webp`, `assets/museum-guide.webp`, `assets/nostos-route.webp` and the new `assets/skald-odyssey-og-070.jpg`. No other route or asset changes, and the old `assets/skald-odyssey-og.jpg` is kept.

- `deployment.json` records the source commit, the hash of each file, and the validation results.
- `review-registry.json`, `approval.md`, `resolved-index.html`, `reviews/` and `candidate/` are the three-role approval and its evidence, identical to the copy in the source repository.
- `.skald-source.json` still describes the older full-tree promotion. It does not describe this scoped copy.
